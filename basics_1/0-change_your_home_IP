#!/usr/bin/env bash
# Configures the Ubuntu server with specific IP mappings:
# - localhost resolves to 127.0.0.2
# - facebook.com resolves to 8.8.8.8

# Create a new hosts file
cp /etc/hosts ~/hosts.new

# Configure localhost to resolve to 127.0.0.2
sed -i 's/127[.]0[.]0[.]1.*localhost/127.0.0.2 localhost/g' ~/hosts.new
grep -q "127.0.0.2.*localhost" ~/hosts.new || echo "127.0.0.2 localhost" >> ~/hosts.new

# Configure facebook.com to resolve to 8.8.8.8
sed -i '/facebook.com/d' ~/hosts.new
echo "8.8.8.8 facebook.com" >> ~/hosts.new

# Replace the hosts file
cp -f ~/hosts.new /etc/hosts 